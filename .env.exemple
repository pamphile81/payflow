# .env.example
APP_ENV=development
SECRET_KEY=change-me

# Base de données (PostgreSQL conseillé en prod)
# Exemple Postgres : postgresql+psycopg2://payflow_user:motdepasse@localhost:5432/payflow_db
# Exemple SQLite (défaut) : sqlite:///payflow.db
DATABASE_URL=sqlite:///payflow.db

# SMTP
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=587
SMTP_USE_TLS=true
SMTP_USERNAME=ton-email@gmail.com
SMTP_PASSWORD=mot-de-passe-application
MAIL_DEFAULT_SENDER=ton-email@gmail.com

# Optionnel
MAX_CONTENT_LENGTH=52428800
